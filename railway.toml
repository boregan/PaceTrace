[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn mcp_server:create_combined_app --factory --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
